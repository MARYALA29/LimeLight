; swagger-ui-react@^5 declares React 16/17/18 as peers but works fine on
; React 19 at runtime. Until upstream bumps the peer range, install in
; legacy peer-deps mode so both `npm install` (local) and `npm ci` (CI)
; resolve the dep tree without silently dropping it.
legacy-peer-deps=true
